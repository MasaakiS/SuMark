ignore-scripts=true
package-lock=true
audit=true

# npm v11.10.0+ only: reject package versions released too recently.
min-release-age=7

# Lockfile is enforced operationally by always using `npm ci` in CI and docs.